import { GetHomeActiveBannerPipe } from './get-home-active-banner.pipe';

describe('GetHomeActiveBannerPipe', () => {
  it('create an instance', () => {
    const pipe = new GetHomeActiveBannerPipe();
    expect(pipe).toBeTruthy();
  });
});
